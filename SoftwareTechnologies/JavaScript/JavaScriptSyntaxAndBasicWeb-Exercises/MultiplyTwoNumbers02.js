function solve(arr) {

    let num1 = Number(arr[0]);
    let num2 = Number(arr[1]);

    function multiply(num1, num2) {

        return num1 * num2;
    }

    let output = multiply(num1, num2);
    console.log(output);
}

