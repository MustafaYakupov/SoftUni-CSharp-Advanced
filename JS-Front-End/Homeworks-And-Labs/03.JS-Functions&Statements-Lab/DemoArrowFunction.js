// Arrow function statement body
const sum = (a, b) => {
    const result = a + b;

    return result;
}

// Arrow function expression body - We can have 1 expession max to be able to use it
const sum2 = (a, b) =>  a + b;

// Expression body arrow function with single parameter
const double = num => num * 2;
