using System;

namespace P01BandMembers
{
    class P01BandMembers
    {
        static void Main(string[] args)
        {
           // decimal members = decimal.Parse(Console.ReadLine());
           // decimal guitarPrice = decimal.Parse(Console.ReadLine());
           // 
           // decimal vocalists = 1;
           // decimal guitarists = Math.Floor(members / 3);
           // decimal drummers = members - vocalists - guitarists;
           // 
           // decimal drumPrice = guitarPrice * 1.50m;
           //
           // decimal totalDrumPrice = drummers * drumPrice;
           // decimal guitarsTotalPrice = guitarPrice * guitarists;
           // decimal microphonePrice = Math.Max(drumPrice, guitarPrice) - Math.Min(drumPrice, guitarPrice);
           // decimal rentPerYear = ((totalDrumPrice + microphonePrice + guitarsTotalPrice) / 3) * 12;
           //
           // decimal total = rentPerYear + drumPrice + guitarsTotalPrice + microphonePrice;
           //
           // Console.WriteLine($"Total cost: {total:f2}lv.");
        }
    }
}
